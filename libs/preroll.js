function admanStat(app_id, user_id){ function addScript(app_id, user_id){ var s = document.createElement("img"); s.style.display = "none"; s.src = "https://js.appscentrum.com/p.jpg?app_id=" + app_id + "&user_id=" + user_id; document.body.appendChild(s); } if(!user_id) VK.api("users.get", {}, function(data){ addScript(app_id, (data && data.response[0] && data.response[0].uid) || "0"); }); else addScript(app_id, user_id); }